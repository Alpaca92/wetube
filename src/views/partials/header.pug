header
  a.header__logo(href="/")
    i.fab.fa-youtube
  nav 
    ul
      li 
        a(href="/search")
          i.fas.fa-search
      if loggedIn
        li 
          a(href="/videos/upload") upload
        li
          a(href="/users/edit") Edit profile
        li
          a(href="/users/logout") Logout
        li
          a(href=`/users/${loggedInUser._id}`)
            if loggedInUser.avatarUrl
              img.header__avatar.avatar(src=`/${loggedInUser.avatarUrl}`)
            else
              span 😀
      else
        li 
          a(href="/login") Login
        li 
          a.header_btn(href="/join") Join
